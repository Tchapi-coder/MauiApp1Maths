<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1Maths.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="maths.png"
                HeightRequest="200"
                HorizontalOptions="Center" />

            <Label
                Text="(a+b)^2= a^2+2ab+b^2"
                FontSize="24"
                HorizontalOptions="Fill" />

            <Entry x:Name="exprEntry"
                     FontSize="24"/>
            <Label x:Name="reslabel"
                   Text="la solution de l equation:"
                   FontSize="24"
                   HorizontalOptions="Fill"/>

            <Button
                x:Name="CounterBtn"
                Text="Calcule moi" 
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
